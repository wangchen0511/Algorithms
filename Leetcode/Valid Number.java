public class Solution {    public boolean isNumber(String s) {        // Note: The Solution object is instantiated only once and is reused by each test case.        boolean startCount = false;        boolean hasPoint = false;        boolean hasE = false;        boolean hasSpaceMid = false;        boolean hasSign = false;        for(int i = 0; i < s.length(); i++){            if(s.charAt(i) == ' '){                if(startCount || hasPoint || hasE || hasSign){                    hasSpaceMid = true;                }            }else if(s.charAt(i) == '-' || s.charAt(i) == '+'){                if(hasPoint || hasSpaceMid || startCount || hasSign){                    return false;                }                   hasSign = true;            }else if(s.charAt(i) == 'e'){                if(hasE || !startCount || hasSpaceMid){                    return false;                }else{                    hasE = true;                    hasPoint = false;                    hasSign = false;                    startCount = false;                }            }else if(s.charAt(i) == '.'){                if(hasPoint || hasSpaceMid || hasE){                    return false;                }else{                    hasPoint = true;                }            }else if(!isNum(s.charAt(i))){                return false;            }else{                if(hasSpaceMid){                    return false;                }                startCount = true;            }        }        if(startCount){            return true;        }else{            return false;        }    }        private boolean isNum(char c){        if(c >= '0' && c <= '9'){            return true;        }else{            return false;        }    }    }